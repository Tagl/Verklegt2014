#pragma once

#define PERSONSORTCOUNT 6
#define COMPUTERSORTCOUNT 5

enum PersonSortTypes { PID = 0, FIRSTNAME, SURNAME, GENDER, DOB, DOD };
enum ComputerSortTypes { CID = 0, NAME, TYPE, WASMADE, YEARBUILT };
enum Order { ASCENDING = 0, DESCENDING };
